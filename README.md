# ffrd-data-model

FFRD Data Model — schema design, user stories, data dictionary, and lakehouse implementation reference for the validation basin studies.

---

## Repository Structure

```
rdb/                        Relational schema (PostgreSQL / PostGIS target)
  ffrd-erd-unified.mmd      Unified domain overview diagram
  events.mmd                Events domain: storms, fishnet points, seeds
  models.mmd                Models domain: models, elements, runs, outputs, linkages
  structures.mmd            Structures domain: dams, levees, buildings, response curves
  management.mmd            Management domain: manifests, plugins, hotfixes, costs, logs

lakehouse/                  Apache Iceberg lakehouse implementation
  ffrd-lakehouse.mmd        Unified domain overview diagram
  events.mmd                Events domain (Iceberg table design)
  models.mmd                Models domain (Iceberg table design + partition annotations)
  structures.mmd            Structures domain (Iceberg table design)
  management.mmd            Management domain (Iceberg table design)
  derived.mmd               Materialized views / derived aggregation tables
  example-mapping.md        Field mapping reference: source JSON → lakehouse tables

data-dictionary.yaml        Single source of truth for all table and column definitions
data-dictionary.md          Rendered Markdown preview (generated by preview_dict.py)
preview_dict.py             Script to regenerate data-dictionary.md from the YAML

user-storeis.md             Persona-centered user stories (33 stories across 12 personas)
user-stories-implementation.md  Data model traceability: how each story is supported
```

---

## Domain Diagrams

The data model is organized into five domains. Each domain has both an **rdb** (relational) and a **lakehouse** (Iceberg) representation.

| Domain | Description | rdb | Lakehouse |
|--------|-------------|-----|-----------|
| Events | Stochastic storm catalog, fishnet spatial grid, realized events, and random seeds | [rdb/events.mmd](rdb/events.mmd) | [lakehouse/events.mmd](lakehouse/events.mmd) |
| Models | Models, model elements, inter-model linkages, runs, time series and gridded outputs, observed gage data | [rdb/models.mmd](rdb/models.mmd) | [lakehouse/models.mmd](lakehouse/models.mmd) |
| Structures | Dams, levees, buildings, fragility response curves, event failure elevations | [rdb/structures.mmd](rdb/structures.mmd) | [lakehouse/structures.mmd](lakehouse/structures.mmd) |
| Management | Manifests, plugins, hotfixes, run logs, resource costs, file artifacts | [rdb/management.mmd](rdb/management.mmd) | [lakehouse/management.mmd](lakehouse/management.mmd) |
| Derived | Materialized aggregation views (lakehouse only) | — | [lakehouse/derived.mmd](lakehouse/derived.mmd) |

Unified overview diagrams:
- [rdb/ffrd-erd-unified.mmd](rdb/ffrd-erd-unified.mmd)
- [lakehouse/ffrd-lakehouse.mmd](lakehouse/ffrd-lakehouse.mmd)

---

## Data Dictionary

[data-dictionary.yaml](data-dictionary.yaml) is the single source of truth for all table and column definitions across all five domains. It defines type, constraints, FK references, and descriptions for every column. The rendered Markdown version is [data-dictionary.md](data-dictionary.md).

To regenerate the Markdown preview after editing the YAML:

```bash
python3 preview_dict.py
```

---

## User Stories

[user-storeis.md](user-storeis.md) contains 33 persona-centered user stories across 12 roles (CC Admin, H&H Engineer, Conformance Lead, Consequences Analyst, etc.), each describing what a user needs and why.

[user-stories-implementation.md](user-stories-implementation.md) maps every story to the specific tables that support it, the query path through the schema, and — where a materialized view exists — the preferred lakehouse query path (marked ⚡).

---

## RDB vs. Lakehouse: Key Differences

### Constraint Enforcement vs. Application-Layer Validation

The relational (`rdb`) schema relies on the database engine to enforce integrity:

- **Primary keys** prevent duplicate rows at the storage level.
- **Foreign keys** guarantee referential integrity — a `run` cannot reference a non-existent `event` or `model`.
- **NOT NULL** constraints prevent incomplete records from being committed.
- **Enum types** restrict fields like `run_type` to a defined set of values.

The Iceberg **lakehouse** schema has no engine-enforced constraints. Apache Iceberg is an open table format built on object storage — it provides ACID transactions, snapshot isolation, and schema evolution, but it does **not** enforce primary keys or foreign keys. This means:

- Uniqueness, referential integrity, and domain validation must be enforced at **write time** by the ingestion pipeline (e.g. a Spark job, a Flink pipeline, or an ETL service).
- Logical relationships are documented in the diagram comments (`%% Join intent:`) and in the data dictionary `references:` field, but they are advisory, not enforced.
- Duplicate detection and orphan checks become responsibilities of data quality tests (e.g. Great Expectations, dbt tests) run as part of the pipeline, not the storage layer.

This is the standard trade-off in lakehouse architectures: you gain horizontal scalability and cost-effective storage at the expense of moving integrity guarantees up the stack into application logic.

---

### Why Iceberg for FFRD Output Scale

The core output tables — `output_ts` and `output_grids_repo` — operate at a scale that is incompatible with a traditional relational database:

| Table | Estimated Scale |
|-------|----------------|
| `output_ts` | ~20,000–200,000 events × ~1,000s of elements × timesteps → **billions of rows** |
| `output_grids_repo` | One grid file per event per variable per run → **hundreds of thousands of Icechunk snapshots** |

Apache Iceberg addresses this scale through several mechanisms:

**Partitioning and file skipping.** `output_ts` is partitioned by `bucket(512, event_id)` and `run_type`. Iceberg maintains a manifest of which data files cover which partition values, so a query for a single event's time series skips all unrelated files entirely — no full-table scan.

**Sort ordering.** Each data file is sorted by `event_id, element_id, variable, ts`. This means a read for a specific element's full time series within an event hits a narrow file range, making the access pattern as efficient as a clustered index in a relational database.

**Snapshot isolation and time travel.** Every write produces an immutable snapshot. A hotfix rerun produces a new snapshot, not an overwrite. Old snapshots remain queryable for audit and comparison, directly supporting the version traceability requirements in the user stories.

**Schema evolution without rewrite.** Adding a column (e.g. `ingest_ts`, `source_system`) does not require rewriting existing data files. This supports iterative schema development as the model matures.

**Icechunk for gridded outputs.** Gridded outputs are stored in [Icechunk](https://icechunk.io) repositories — a chunked array store designed for Zarr/NetCDF-style data with Iceberg-compatible snapshot semantics. `output_grids_repo` stores metadata pointers (repository URI + snapshot reference) rather than blob data, keeping the Iceberg table lean while the heavy array data lives in the chunked store.

---

## Materialized Views

The [derived domain](lakehouse/derived.mmd) defines seven materialized views that pre-aggregate the high-volume source tables for common query patterns:

| View | Source | Purpose | Stories |
|------|--------|---------|---------|
| `mv_peak_output_by_element` | `output_ts` | Peak value per element per run | 15, 24, 28 |
| `mv_event_variable_stats` | `output_ts` | Mean/max/min/std across elements per event | 6, 32 |
| `mv_aep_frequency_stats` | `output_ts` + `events` | Frequency statistics (p10/p50/p90) per AEP band | 11, 28, 31, 32 |
| `mv_gage_model_comparison` | `output_ts` + `obs_ts` | Observed vs. modeled peak bias per gage | 15 |
| `mv_run_performance` | `run_catalog` + `run_resources` | Duration, cost, and status per run | 2, 3 |
| `mv_basin_cost_rollup` | `run_resources` + `run_catalog` | Monthly cost and utilization rollup per basin | 4 |
| `mv_conformance_summary` | `models.metadata_json` | Conformance check pass/fail per model version | 5 |

These views are **not source-of-truth** — they are regenerated after each batch commit and serve as read-optimized accelerators. Without them, answering questions like "what is the 100-year peak flow at this junction across all realizations?" would require scanning billions of `output_ts` rows at query time.

---

## Gridded Data — Icechunk Repositories

Gridded (raster) observed data and model outputs are stored in [Icechunk](https://icechunk.io) repositories rather than as relational tables. Icechunk is a chunked, cloud-native array store built on [Zarr](https://zarr.dev) conventions that provides Iceberg-compatible snapshot semantics and efficient multidimensional indexing. Metadata pointers to Icechunk repositories live in the relational schema (`observed_grids` and `output_grids` tables); the actual gridded data — characterized by dimensions like longitude, latitude, time, and variable — lives in long-lived repos that accumulate snapshots over time. With Iceberg, these datasets can be updated incrementally in-place (with data version control) if the storm catalog changes or events need to be re-run.

See [rdb/grids.mmd](rdb/grids.mmd) for a conceptual diagram of the three repository types and how they relate to the relational catalog tables.

### Repository Types

**1. `observed_precip` — Observed Meteorology from NOAA AORC**

Real-world precipitation and temperature observations; i.e., the raw storm catalog data.
* `storm_id` is a dimension for efficient slicing by storm.
* `time` is measured relative to the start of each storm.
* `abs_time` is the real-world time of occurence.

```
<xarray.Dataset> Size: 1TB
Dimensions:    (storm_id: 440, time: 73, y: 1014, x: 2000)
Coordinates:
  * storm_id  (storm_id) int64 4kB 1 2 3 4 5 6 ... 436 437 438 439 440
  * time      (time) timedelta64[ns] 584B 00:00:00 ... 3 days 00:00:00
  * y         (y) float64 8kB 4.969e+06 4.966e+06 ... 2.129e+06
  * x         (x) float64 16kB -1.067e+06 -1.064e+06 ... 4.537e+06
    abs_time  (storm_id, time) datetime64[ns] 257kB 2016-03-09 ... 2...
Data variables:
    precip    (storm_id, time, y, x) float64 521GB
    temp      (storm_id, time, y, x) float64 521GB
Attributes:
    source: NOAA AORC
    crs: WGS 84 (EPSG:4326)
```

Cataloged by: `observed_grids` table

---

**2. `hms_excess_precip` — HEC-HMS Synthetic Excess Precipitation**

Gridded excess precipitation output from HEC-HMS runs, merged across all SST events.
* `event_id` is a dimension for efficient slicing by event.
* `time` is measured relative to the start of each event.

```
<xarray.Dataset>
Dimensions:         (event_id: 20000, lon: 2500, lat: 2500, time: 72)
Coordinates:
  * event_id        (event_id) int64 1 2 3 4 5 6 ... 20000
  * lon             (lon) float32 -97.5 -97.49 ... -87.5
  * lat             (lat) float32 24.5 24.51 ... 34.5
  * time            (time) timedelta64[ns] 00:00:00 ... 3 days 00:00:00
Data variables:
    excess_precip   (event_id, time, lat, lon) float32 ...  [unit: in]
Attributes:
    project: Big River Basin (FFRD Validation)
    model: HEC-HMS
    crs: FFRD-Projection
```

Cataloged by: `output_grids` table

---

**3. `ras_output` — HEC-RAS Maximum Depth and Velocity**

Peak flood depths and velocities from HEC-RAS runs, merged across all runs for each event (no time dimension — spatial maximum values only). 
* `event_id` is a dimension for efficient slicing by event.

```
<xarray.Dataset>
Dimensions:       (event_id: 20000, lon: 5000, lat: 5000)
Coordinates:
  * event_id      (event_id) int64 1 2 3 4 5 6 ... 20000
  * lon           (lon) float32 -97.5 -97.49 ... -87.5
  * lat           (lat) float32 24.5 24.51 ... 34.5
Data variables:
    max_depth     (event_id, lat, lon) float32 ...  [unit: ft]
    max_velocity  (event_id, lat, lon) float32 ...  [unit: ft/s]
Attributes:
    project: Big River Basin 
    model: Upper Big River
    crs: FFRD-Projection
```

Cataloged by: `output_grids` table

---

## Early Iterations

![Iceberg Data Process](prior-art/iceberg-plugin-data-process.png)
![Iceberg Production Data](prior-art/iceberg-production-data.png)
